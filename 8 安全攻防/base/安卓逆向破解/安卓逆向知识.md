```
#!/usr/bin/env bash

echo "----------------------设备相关----------------------"
echo "activity查看:adb shell dumpsys activity | grep -E 'mResumedActivity|mFocusedActivity'"
echo "selinux查看:getenforce,设置:setenforce 1"
echo "abi查看:getprop ro.product.cpu.abi,设置setprop ro.debuggable 1"
echo "debug设置:https://bbs.pediy.com/thread-215311.htm,兼容4.x-7.x"
echo "    推送文件:adb push /Library/Android/bin/libs/prop/mprop /data/local/tmp"
echo "    进入终端:adb shell"
echo "    超级权限:su"
echo "    设置权限:chmod 755 /data/local/tmp/mprop"
echo "    运行逻辑:/data/local/tmp/mprop"
echo "    设置属性(这里设置debug属性):setprop ro.debuggable 1"
echo "    恢复正常(不能再修改属性值，但已修改的有效):/data/local/tmp/mprop -r"
echo "    查看设置属性:getprop ro.debuggable"
echo ""

echo "----------------------签名相关----------------------"
echo "打包流程:open /Library/Android/bin/libs/build.png"
echo "工具集合:open /Library/Android/build-tools/28.0.3"
echo "签名生成:keytool -genkey -alias <alias> -keystore <file> -keyalg RSA -validity 36500"
echo "签名剔除:zip -d <apk> META-INF/*.RSA META-INF/*.SF"
echo "签名重签:jarsigner -verbose -keystore <keyfile> -signedjar <resignapk> <unsignapk> <keyalias>"
echo "应用对齐:zipalign -v 4 <resignapk> <zipapk>"
echo ""

echo "----------------------资源相关----------------------"
echo "资源概要:aapt d badging <apk>"
echo "资源文件:aapt d xmltree <apk> <file>"
echo "资源列表:aapt l -v [-a] <apk>"
echo "资源添加:aapt a -v <apk> <file1> [file2...]"
echo "  zip追加:zip -g <zipfile> <file>"
echo "资源删除:aapt r -v <apk> <file1> [file2...]"
echo "  zip删除:zip -d <zipfile> <reg1> [reg2...]"
echo "  zip更新:zip -u <zipfile> <file>"
echo ""

echo "----------------------代码相关----------------------"
echo "https://github.com/JesusFreke/smalidea:version(2.4.0)"
echo "代码编译:javac -source 1.6 -target 1.6 <javafile> -d <savedir>"
echo "dex编译:dx --dex --output=<dex> <classfile>"
echo "dex反编:java -jar /Library/Android/bin/libs/smali/baksmali.jar d <dex> -o <dir>"
echo "dex重编:java -jar /Library/Android/bin/libs/smali/asssmali.jar a <dir> -o <dex>"
echo ""


```

